# Moved

SQL migrations for the shared `serveaso` database now live in the repo root:

**[`../../../../../database/sql/`](../../../../../database/sql/)**

Add new files there as `NNN_description.sql` and run `npm run db:migrate` from the monorepo root.

This folder is kept empty for backwards compatibility with old docs paths.
